<%= form_with(model: participation, local: true) do |form| %>
  <% if participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(participation.errors.count, "error") %> prohibited this participation from being saved:</h2>

      <ul>
      <% participation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content_id %>
    <%= form.text_field :content_id, id: :participation_content_id %>
  </div>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_field :user_name, id: :participation_user_name %>
  </div>

  <div class="field">
    <%= form.label :is_recommend %>
    <%= form.check_box :is_recommend, id: :participation_is_recommend %>
  </div>

  <div class="field">
    <%= form.label :is_public %>
    <%= form.check_box :is_public, id: :participation_is_public %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
